# AGENTS.md

MTurrets is a Kotlin NeoForge mod (MC 1.21.1) porting Mindustry's turret, machine, and material gameplay into Minecraft. Mod id, resource namespace, and package are all `mturrets` (package `xyz.luobo.mturrets`). Key platform / identity / dependency trade-offs live in `docs/adr/0001-renovation-decisions.md`;

## Build & run

- JDK 21; `gradlew build` builds, `gradlew runClient` smoke-tests the client
- Version coordinates are centralized in `gradle/libs.versions.toml`; runtime deps are only Flywheel / KotlinForForge / Jade (rationale in ADR-0001 §3, superseded by ADR-0002 for the renderer)
- `gradlew runData` regenerates `src/generated/resources` — this directory is committed, and CI gates its freshness with `git diff --exit-code`. After touching registries, block/item models, or lang keys, re-run and commit the output or CI fails
- `gradlew runGameTestServer` runs the behavior regression suite (damage / energy / output, external behavior only); run it locally, CI does not
- The Chinese mirror repositories in settings.gradle are pre-existing environment; keep them

## Language & naming

- Player-visible text goes through lang files: `zh_cn.json` is hand-maintained, `en_us.json` is generated by datagen. Adding visible content = edit `zh_cn.json`, then `runData` to refresh `en_us.json`
- Domain vocabulary follows the `CONTEXT.md` glossary (honor each entry's `_Avoid_` synonyms)

## Reference source (`ref/`)

- Reference / upstream source code lives in the repo-root `ref/`, gitignored and therefore absent on a fresh clone. Mindustry content and assets: `ref/mindustry/` (turret/bullet/material content defs under `ref/mindustry/core/src/mindustry/content/`, sprites / sounds / bundles under `ref/mindustry/core/assets/`); NeoForge / Flywheel / Create deobfuscated source under `ref/src/`. Consult it for Mindustry stats, lead aiming, original art, and non-trivial NeoForge design calls.
- Keep every referenced third-party source inside `ref/` — never copy upstream code into `src/` or anywhere else in the repo.

## Asset production workspace

- `assets/` is the local (gitignored) working area for asset production: Blockbench `.bbmodel` projects and raw PNGs live in `assets/mturrets/`. Nothing under `assets/` is committed, so it is absent on a fresh clone.
- `assets/export/` holds finished, usable exported model assets; to ship one, copy it into `src/main/resources/assets/mturrets/` (committed) and re-run `runData` if a lang or registry key changed.

## Third-party skills

- Third-party agent skills are not committed: `.agents/` is gitignored, rebuild it after cloning with `npx skills experimental_install` (driven by the committed `skills-lock.json`)

## Agent skills

### Issue tracker

Issues and specs live as GitHub issues, operated via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical triage labels, each string equal to its name: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` plus `docs/adr/` at the repo root. See `docs/agents/domain.md`.
